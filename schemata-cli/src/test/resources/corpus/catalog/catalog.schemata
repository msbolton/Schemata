/// Storefront catalog.
namespace corpus.catalog

/// A product for sale.
@proto(name = "Item")
@deprecated
record Product {
  /// Stock keeping unit.
  @proto(name = "sku_code")
  #1 sku: string
  @deprecated("use sku")
  #2 legacy_id: int32?
  @sql(key)
  #3 id: uuid
}
