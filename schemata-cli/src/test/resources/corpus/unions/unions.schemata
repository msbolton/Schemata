namespace corpus.unions

record Card { #1 last4: string(max = 4) }
record Cash {}

union Payment = Card | Cash | uuid

record Order {
  @sql(key) #1 id: uuid
  #2 payment: Payment
  #3 refund: Payment?
  @sql(strategy = json) #4 archived: Payment
}
