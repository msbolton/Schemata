syntax = "proto3";

package corpus.shadow;

message Card {
  bool top = 1;
}

message Order {
  .corpus.shadow.Card outer = 1;
  Card inner = 2;

  message Card {
    bool nested = 1;
  }

  message Line {
    .corpus.shadow.Card again = 1;
    Card near = 2;
  }
}
